{
  "name": "compra-verificada",
  "private": true,
  "scripts": {
    "setup": "npm install && npm install --prefix server && npm install --prefix client",
    "db:ensure": "npm run db:ensure --prefix server",
    "db:bootstrap-remote": "npm run db:bootstrap-remote --prefix server",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "vercel-build": "npm run build",
    "start": "npm run start --prefix server"
  },
  "dependencies": {
    "@vercel/node": "^5.5.9",
    "pg": "^8.20.0",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
